# 03 · Demo 关卡规格

> v1.2 · 五个部分：序章 / Home 主界面 / 选课关 / 海报关 / 学期结算。
> v1.1 变更：行动板升级为 Home 主界面（B 节）；选课/海报改为主线必修不耗行动点；结算含剩余行动点转化。
> v1.2 变更：叙事屏改为**视觉小说（VN）演出**——场景背景 + 角色立绘 + 底部对话框，点击推进；选课关三轮问答内容全面加厚。

## 0.1 新手引导（面向刚拿到电脑的准大一用户）
- **Home 首次进入**：五步聚光灯引导（主线任务→选修行动板→右侧状态栏→左侧导航→结算按钮），气泡含标题/说明/进度点，可随时跳过；顶栏「？新手引导」可重看。已看标记存 localStorage（`unisim_tour_v1`，设备级，不进游戏存档）
- **散点提示**：VN 首次游玩显示「单击画面任意处继续」（推进一次后消失）；选课关文件面板在待拖拽时显示保姆级拖拽说明（按住不松手→移动→松开）；海报工作台顶部有 ①→④ 步骤条
- **开发调试**：左下角固定「⟲ 重置进度 · 测试」按钮，一键清档回开局并重置引导标记（正式版移除）

## 0. VN 演出（引擎能力，所有关卡可用）
- dialogue/choice/input 屏，只要在关卡 JSON 的 `copy` 里配 `scene-{屏id}`（指向 presetAssets 的背景）即进入 VN 模式；`sprite-{屏id}` 配立绘（学长在右、NPC 在左）。
- 交互：点击场景任意处 → 先跳过打字机、再前进；choice 的选项浮层居中；input 在对话盒内输入（回车可确认）。
- 说话人名牌：学长为强调色，NPC 为墨色；system 无名牌居中旁白。
- 场景/立绘资产（`/public/assets`，占位可同名替换）：`bg-desk`（书桌通知书）/ `bg-gate`（校门）/ `bg-classroom`（教室）/ `bg-club`（社团活动室）/ `bg-dorm`（宿舍夜）；`sprite-senpai`（学长立绘）/ `sprite-xuejie`（学姐立绘）/ `senpai.svg`（学长头像）。
> 台词均为可用初稿，后续由内容负责人在 /content JSON 中直接润色，不动代码。
> 变量插值：{playerName} {majorName} {clubName}。

---

## A. 序章 prologue

### 流程
```
S1 开场 → S2 输入姓名 → S3 选专业 → S4 立flag(×4屏或一屏四组) → S5 绑定学长系统
→ S6 专业卡片揭示 → S7 文件夹引入 → 进入行动板
```

### 逐屏
| 屏 | type | 内容 | 交互/映射 |
|---|---|---|---|
| S1 | dialogue(system) | 「2026年8月。你的录取通知书到了。」 | [拆开] → S2 |
| S2 | input | 「通知书上的名字是——」 | inputKey=playerName，≤8字 → S3 |
| S3 | choice(搜索选择器) | 「录取专业：」 | 从 majors.json 搜索选择，写入 majorId → S4。无结果→兜底通用卡 |
| S4 | choice×4 | 「入学之前，先回答一个没人会问你的问题：四年后，你想要什么样的生活？」四组按钮：年薪区间 / 城市 / 工作类型 / 下班时间 | 全点选，写入 flag → S5。文案基调：认真但不施压，最后一行小字「随便选。反正你会忘。」 |
| S5 | dialogue(senpai) | 「叮——学长系统绑定成功。」「别紧张，我不是客服。我是四年前坐在你这个位置的人。」 | [你是谁？] → S6 |
| S6 | reveal | 专业卡片：从 majors.json 渲染 {majorName} 的五栏卡片 | effects: typewriter + fakeLoading(1500ms, tips:["学长翻了翻他的旧笔记…"])。底部小字「第 1 件 · 已放进文件夹」→ 档案入库 |
| S7 | dialogue(senpai) | 「我毕业时留下了一个文件夹，里面是我四年做过的每一件东西。」「现在它是你的了。往里装点你自己的吧。」 | [开始大一] → onComplete |

### 结果
- deltas: 无；abilityUnlocks: 无
- archiveItems: [{ id:'major-card', levelId:'prologue', title:'我的专业卡片', resumeLine:'', borrowed:false, assetRef:'major-card' }]

---

## B. 大一上 · Home 主界面 y1s1（v1.1：行动板升级为游戏 hub）

### 布局（引擎实现，非关卡）
- 顶栏：产品名 + `大一上 · 9月` + 玩家名·专业 + 账号（邮箱/登出；本地模式显示徽标）
- 左侧导航：**本学期 / 文件夹 / 属性 / 记录** 四个视图 + 四年时间轴（当前学期高亮，后续灰显）
- 右侧状态栏（常驻）：行动点 ●●● + 五轴条（含精力）+ 入学 flag 卡 + 已解锁 AI 能力
- 中间内容区按导航切换：

**本学期视图**
- 主线任务面板（必修·教学，不耗行动点，顺序解锁）：

| 顺序 | 关卡 id | label | tag | 解锁条件 |
|---|---|---|---|---|
| 1 | course-select | 选课周 | 教学 · 投喂长文档 | 开局即解锁 |
| 2 | poster | 给社团做招新海报 | 教学 · 生图与迭代 | 完成选课周 |

- 选修行动板（行动点 3，完成第一个主线后解锁，锁定文案「先把课选了，行动板再解锁」）：

| 行动 id | label | 耗点 | deltas | 类型 |
|---|---|---|---|---|
| election | 竞选班委 | 1 | expression+2, energy-1 | 速结 |
| gpa | 泡图书馆刷绩点 | 1 | academic+2, energy-1 | 速结 |
| tutor | 接一份家教 | 1 | cash+2, energy-1 | 速结 |
| rest | 什么都不做 | 0 | energy+2 | 速结 |

- [结束这学期]：**主线全部完成后亮起**（行动点可有剩余，结算时剩余点转化为精力 +1/点，提示文案先行展示）
- 速结演出：点击 → 数值跳动 + resultText 一句话 + 学长点评（可空）。示例 rest 的 resultText：「你睡了很多觉，看了两部电影。没有任何产出。挺好的。」

**文件夹视图**（v1.3 升级为三板块沉淀系统）：
- **作品集**：交付型产出（海报、将来的个人主页/简历…），横向堆叠大图卡；详情展示完整作品 + 「简历里可以这样写」措辞
- **提示词库**：每完成一个教学关，自动入库一条可复用提示词模板（关卡 onComplete 追加 `prompt-{能力id}` 档案条目）。模板用 `【填：xxx】` 标记填空槽位，详情页高亮槽位 + 「什么时候用」说明 + 一键复制
- **档案**：记录类产出（专业卡片、避坑清单…），瀑布流信纸卡
- **详情不删减**：避坑清单详情 = 要点卡 + 三轮完整问答记录（提示词原文 + AI 全文回答）；专业卡片 = 完整五栏
- 条目分区在 `content/folder/folder.json` 配置（id → works/prompts/docs，未登记的按 id 前缀 prompt- 归提示词库，其余归档案）
- 结局卡「你自己做了 N/M 件」只统计作品与档案，提示词模板不计入
**属性视图**：五轴大条 + 已解锁能力
**记录视图**：PlayerState.log 倒序时间线（入学/关卡/速结/学期流转/能力解锁）

### 关卡页面（独立于 Home）
- 顶栏：[← 返回主页]（确认弹层：中途离开不保存关卡内进度）+ 关卡名 + tag
- 关卡完成 onComplete → 回 Home，主线卡片打钩

---

## C. 选课关 course-select（主线必修 · 不耗行动点 · 教学：投喂长文档）

> v1.2 重做：改为**拟真 AI 对话工作台**。使用真实文档（浙江大学《2021级计算机科学与技术专业培养方案》与《学生手册》），
> 三轮问答的回答是**预先用 AI 对真实文档跑出的分析结果**（含表格），存于 /content，运行时零 AI 调用。

### 流程
```
S1 教务处通知 → S1b 学长引入 → S2 AI 对话工作台（三轮问答）→ S6 交付避坑清单 → 完成
```

### S2 AI 对话工作台（宽版面，自定义屏）
- 左侧「本地文件」面板：两张文件卡（培养方案.pdf 8页 / 学生手册.pdf 218页），**HTML5 真实拖拽**进右侧对话框（双击为无障碍降级入口，不做视觉引导）
- 右侧 AI 对话框：标题栏 + 消息流 + 输入区，拖拽悬停时高亮
- 三轮问答（预设提示词以打字动画输入 → 思考动画 → 回答流式输出，点击气泡跳过）：

| 轮 | 前置 | 提示词（长提示词：身份+分点要求+「不许编」约束） | 回答要点（预跑结果，Markdown 表格化） |
|---|---|---|---|
| ① | 拖入培养方案 | 学分结构 + 大一分学期选课清单 + 按严重程度排序的坑 | 学分结构总表（156+7.5+6+8 六大块）；秋冬/春夏/短学期三张课表（含优先级列）；5 个坑排序；「需到校确认」清单 |
| ② | 拖入学生手册 | 四年关键规则速查（绩点换算表/挂科后果/学籍红线/转专业/保研/奖助学金/科研竞赛，注明出处） | 百分制→绩点换算表（5 分制）；五种情况后果表；学籍红线表（12/55/80 学分）；转专业窗口表（两门≥4.2、均绩≥4.0）；保研条件表+时间线；八档奖助学金表（可否叠加）；SRTP/竞赛入口；每节标注来源文件 |
| ③ | — | 把前两问分析变成 8 周暑假准备计划（C 语言/英语四级/规则落地） | 三条线阶段目标表；八周计划表（每周目标+免费资源+自查成果）；开学第一周行动清单（可勾选） |

- 每轮之间穿插学长旁白（带头像）；三轮完成 → [整理成避坑清单] → S6
- 回答内容全部在 course-select.json copy 的 q1/a1/q2/a2/q3/a3（Markdown 字符串，支持表格/列表/加粗）

### 结果
- deltas: 无；abilityUnlocks: ['doc-feeding']
- archiveItems: [{ id:'course-map', levelId:'course-select', title:'大一避坑清单', resumeLine:'', borrowed:false }]
- 完成后选修行动板解锁，带折扣的行动显示 ⚡ 角标
- 教学口诀不变：**别读长文件，喂长文件**

---

## D. 海报关 poster（主线必修 · 深度 · 教学：生图+迭代 · 全预设演出 · 不耗行动点）

### 流程
```
S1 学姐派活 → S2 任务面板 → S3 喂料(社团名/风格) → S4 假生成 → S5 首版出图+部分打钩
→ S6 学姐驳回 → S7 修改选择(提示词可视化) → S8 二版出图 → S9 16:9版 → S10 交付
任意时刻可点 [问学长] → ESC 屏 → 直接入库学长成品(borrowed)
```

### 验收清单 checklist
| id | label |
|---|---|
| ck-ratio | 3:4 竖版 |
| ck-space | 标题区留白在上方 |
| ck-name | 出现社团名 |
| ck-169 | 追加 16:9 版 |

### 逐屏
| 屏 | type | 内容 | 交互/映射 |
|---|---|---|---|
| S1 | dialogue(npc:学姐) | 「周五要用招新海报。3:4，贴宣传栏。标题那块给我留空，字我们自己压。要有社团名。」 | [接下] → S2 |
| S2 | dialogue(senpai) | 任务面板亮起(4项)。「先别急着跟AI说『好看一点』。她要的三件事，就是你提示词的三句话。」 | [开始] → S3 |
| S3 | workbench | ①社团名 input(inputKey=clubName，默认值「星野动漫社」可直接用) ②风格三选一缩略图(极简/手绘/摄影)→setVar style ③尺寸按钮[3:4][16:9][1:1]，选3:4→checkItem ck-ratio。旁注小字：「尺寸是参数，不用写进提示词——构图才需要你说话」 | [生成] → S4 |
| S4 | reveal | fakeLoading(3000ms, tips:["真实出图大概要20秒，习惯它","提示词已带上：上三分之一留白，主体在下"]) | → S5 |
| S5 | reveal | showAsset:"poster-{style}-34-v1"，社团名以文字层叠加(→checkItem ck-name)。学长：「注意，它没完全听你的——主体有点满。真实的AI就这样。所以才有下一步。」 | [交给学姐] → S6 |
| S6 | dialogue(npc:学姐) | 「主体太满了，标题没地方放。另外公众号头图还要一版16:9的。」 | [改] → S7 |
| S7 | choice | 三个修改按钮：[主体往下压] [背景压暗] [保持构图再抽一次]。**右侧同步展示提示词变化**：选中项以高亮文字追加到原提示词尾部（预写文案） | 任选→setVar revision + checkItem ck-space → S8 |
| S8 | reveal | fakeLoading(2000ms) → showAsset:"poster-{style}-34-v2" | [出16:9版] → S9 |
| S9 | reveal | 「同一句提示词，只换一个参数。」→ showAsset:"poster-{style}-169"，checkItem ck-169 | [交付] → S10 |
| S10 | deliver | 双图并排入库动画，清单4/4。学姐：「行，比上届强。」学长：「当年这张我改了三遍。你比我快。」+ 解锁按钮[看学长当年的完整提示词]（展开预写提示词全文） | onComplete |
| ESC | deliver | 学长：「卡住了？拿我那版去交吧。但答应我，下学期自己做一张。」入库学长成品 | onEscape，borrowed:true |

### 结果
- deltas: 按清单：4/4→portfolio+2；2~3→portfolio+1；ESC→portfolio+1
- abilityUnlocks: ['image-gen']
- archiveItems: [{ id:'poster-y1', levelId:'poster', title:'社团招新海报 · 双版本', resumeLine:'独立完成社团宣传物料设计（3:4/16:9双版本）', borrowed:视路径 }]

---

## E. 学期结算 settlement

### 流程
```
S1 学期小结(数值动画) → S2 档案一览 → S3 简易结局卡 → S4 结尾钩子
```

| 屏 | 内容 |
|---|---|
| S1 | 「大一上 · 结束」四轴增长动画 + 本学期行动回放列表（每行一句 resultText）。剩余行动点在进入结算时转化为精力（+1/点），记录写入 log |
| S2 | 文件夹视图：已入库条目（含 borrowed 标记显示为「借来的」小标签） |
| S3 | 简易结局卡：①顶部原样放回开局 flag「入学前，你说你想要——」②四轴形状图 ③一句判词（预写规则：取最高轴，各写一句；全低且精力高→「休整型开局」判词，同样体面）④「这学期 N 件作品里，你自己做了 M 件」 |
| S4 | 学长：「这才是第一个学期。」灰显的后续学期时间轴（大一下…毕业）+「Demo 到这里。想看后面的，告诉学长。」 |

---

## 5. 预设资产清单（占位先行）
| 资产 id | 数量 | 说明 |
|---|---|---|
| poster-{jj/sh/sy}-34-v1 | 3 | 极简/手绘/摄影 × 首版 3:4（主体偏满） |
| poster-{style}-34-v2 | 3 | 修正版（上三分之一留白明显） |
| poster-{style}-169 | 3 | 16:9 版 |
| poster-senpai-34 / -169 | 2 | 学长成品（ESC 用，含完成态文字排版） |
| major-card 背景 | 1 | 专业卡片底纹 |
**开发期全部用占位 SVG（标注资产 id 与规格文字），文件名即资产 id。真图就位后同名替换。**
v1 与 v2 的视觉差异必须肉眼可辨（主体位置/留白），占位 SVG 也要体现这个差异（如色块位置不同）。

## 6. 内容线待办（不阻塞开发）
- majors.json 30~50 个专业（来自现有志愿填报知识库）
- 海报真图 11 张（与占位同名替换）
- 全部台词润色（直接改 /content JSON）
