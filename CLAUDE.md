# CLAUDE.md · 《大学模拟器》(暂定名) Demo

## 项目是什么
一个大学四年模拟器网页游戏：决策层（学期卡+行动点+选择）+ 执行层（AI 教学关卡）。
Demo 范围：序章 → 大一上学期（选课关 + 行动板 + 海报关 + 若干速结行动）→ 学期结算。
详见 docs/01_产品定义.md。**开发任何功能前先读 docs/ 下三份文档。**

## 技术栈（不得擅自更换）
- React 18 + Vite + TypeScript，纯静态 SPA，**无后端、无任何运行时 AI 调用**
- 状态管理：Zustand（单 store 持有 PlayerState）
- 样式：Tailwind CSS
- 存档：localStorage（带 version 字段）
- 部署产物：静态文件，`npm run build` 即全部

## 目录结构（冻结）
```
/src
  /contracts        ← 类型契约，对应 docs/02。【只读，发现不够用时停下报告，禁止自行扩展】
  /engine           ← 引擎：路由/状态机、学生面板、档案、存档、内容加载器
  /levels           ← 每个关卡一个目录，作为插件实现 LevelModule 接口
    /prologue
    /course-select
    /poster
    /settlement
  /components/ui    ← 通用 UI（按钮、卡片、任务面板、HUD）
/content            ← 全部中文文案与关卡配置 JSON。代码组件内禁止出现任何硬编码中文文案
  /levels           ← 每关一个 json，格式见 docs/02
  /majors           ← 专业卡片预生成库
  /quick-actions    ← 速结行动配置
/public/assets      ← 预设图片资产，命名规则见 docs/03
/docs               ← 01 产品定义 / 02 数据契约 / 03 关卡规格
```

## 铁律
1. **内容与代码分离**：任何面向用户的中文字符串一律来自 /content 下的 JSON。违反即返工。
2. **关卡是插件**：只通过 src/contracts 中的 LevelModule / LevelResult 接口与引擎通信，关卡之间零依赖。
3. **契约冻结**：/src/contracts 与 docs/02 保持一致；需要新字段时，先在回复中提出方案等确认，不要直接改。
4. **无网络请求**：demo 不发任何 fetch。所有"AI 生成"均为预设内容的演出（打字机效果、延迟揭示等）。
5. **资产占位**：真实海报图未就位前，用生成的占位 SVG（标注资产 ID），文件名严格按 docs/03 的命名规则，后续直接替换文件即可，不改代码。
6. **移动端优先**：主要用户在手机上打开（微信内浏览器），375px 宽度优先设计，桌面端只需不破版。
7. **每完成一个任务**：跑 `npm run build` 确认无报错，并简述改动了哪些文件。

## 视觉基调
极简、克制、留白多、单色系 + 一个强调色；禁止赛博朋克渐变、禁止花哨动效。
游戏 HUD 元素（行动点、四轴、任务清单、档案槽位）要有"游戏感"但保持干净。
参考气质：文件夹 / 档案 / 信纸，而非霓虹 / 科技风。

## 术语表
| 术语 | 含义 |
|---|---|
| 学长 | 唯一指导角色，所有系统提示的皮。开局台词"叮——学长系统绑定成功" |
| 行动点 | 每学期发放的时间货币，demo 大一上为 3 点 |
| 四轴 | 学术/作品/表达/现金，另有隐藏轴精力 |
| 档案 | 玩家产出物的记录，驱动简历/结局的个性化 |
| 借来的 | 玩家点[问学长/直接看学长那版]跳过后，档案条目的标记 borrowed:true |
| 速结行动 | 10 秒内完成的纯数值行动，无深度玩法 |
| 深度关卡 | 有逐屏流程的教学关卡（demo 中为海报关） |
