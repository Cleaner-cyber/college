# CLAUDE.md · 《大学模拟器》(暂定名)

## 项目是什么
一个大学四年模拟器网页游戏（桌面端），双重目的：
1. **大学规划**：让准大一新生把大学四年先"过一遍"，学会分配时间、做取舍；
2. **AI 教学**：用交互式教学关，教零基础新生在真实校园场景里用 AI 提效（做海报/PPT/读长文档…）。

当前范围：序章 → 大一上学期（Home 主界面 + 主线教学关×2 + 选修速结行动）→ 学期结算。
**开发任何功能前先读 docs/ 下文档（01 产品定义 / 02 数据契约 / 03 关卡规格 / 05 工程架构 / 07 后端）。**

## 技术栈（不得擅自更换）
- 前端：React 18 + Vite + TypeScript + React Router（HashRouter）
- 状态管理：Zustand（单 store 持有 PlayerState）
- 样式：Tailwind CSS，**桌面端优先**（1280px 基准，1024px 不破版）
- 后端：Supabase 直连（邮箱+密码登录、Postgres 云存档、RLS 行级权限）
  - 未配置 Supabase 环境变量时自动降级「本地试玩模式」（localStorage，无登录）
- 部署产物：静态文件（`npm run build`）+ 一个 Supabase 项目（结构见 supabase/schema.sql）

## 目录结构
```
/src
  /contracts        ← 类型契约，对应 docs/02。【只读，发现不够用时停下报告，禁止自行扩展】
  /engine           ← 引擎：App路由/store/ScreenPlayer/内容加载器/建档服务/关卡注册表
  /services         ← Supabase 客户端 / 认证 store / 存档适配器（云端+本地）
  /pages            ← 登录 / 序章 / Home主界面 / 关卡页 / 结算页
  /levels           ← 每个关卡一个目录，作为插件实现 LevelModule 接口
  /components/ui    ← 通用 UI（按钮、面板、任务清单、打字机、假加载）
/content            ← 全部中文文案与关卡配置 JSON。代码组件内禁止出现任何硬编码中文文案
/public/assets      ← 预设图片资产，命名规则见 docs/03
/supabase           ← schema.sql（数据库结构与 RLS 策略）
/docs               ← 01-07 全部文档
```

## 核心结构（游戏形态）
- **Home 主界面**（游戏 hub）：左侧导航（本学期/文件夹/属性/记录 + 四年时间轴）、中间内容区、右侧状态栏（行动点/五轴/四年目标/已解锁能力）。
- **关卡页面**：独立于 Home 的沉浸式页面，做完回 Home。
- **主线必修**（教学关）：不耗行动点、顺序解锁、**全部完成才能结算学期**——保证每个玩家都经历 AI 教学，这是产品底线。
- **选修速结**：行动点只花在这里（每学期 3 点），制造取舍感。
- 结算时剩余行动点自动转化为休息（精力 +1/点），不惩罚不浪费。

## 铁律
1. **内容与代码分离**：任何面向用户的中文字符串一律来自 /content 下的 JSON。违反即返工。
2. **关卡是插件**：只通过 src/contracts 中的 LevelModule / LevelResult 接口与引擎通信，关卡之间零依赖，关卡不 import store。
3. **契约冻结**：/src/contracts 与 docs/02 保持一致；需要新字段时，先在回复中提出方案等确认，不要直接改。
4. **网络请求白名单**：只允许访问 Supabase（认证与存档）。关卡内所有「AI 生成」仍是预设内容的演出（打字机、假加载、预设图），不接任何真实 AI API（个人主页关 #15 除外，不在当前范围）。
5. **资产占位**：真实海报图未就位前，用生成的占位 SVG（标注资产 ID），文件名严格按 docs/03 的命名规则，后续直接替换文件即可，不改代码。
6. **每完成一个任务**：跑 `npm run build` 确认无报错，并简述改动了哪些文件。

## 视觉基调
极简、克制、留白多、单色系 + 一个强调色；禁止赛博朋克渐变、禁止花哨动效。
游戏 HUD 元素（行动点、五轴、任务清单、档案）要有"游戏感"但保持干净。
参考气质：文件夹 / 档案 / 信纸，而非霓虹 / 科技风。详见 docs/04。

## 术语表
| 术语 | 含义 |
|---|---|
| 学长 | 唯一指导角色，所有系统提示的皮。开局台词"叮——学长系统绑定成功" |
| 主线必修 | 教学关卡：不耗行动点、顺序解锁、不完成不能结算学期 |
| 行动点 | 每学期发放的时间货币（大一上 3 点），只用于选修速结行动 |
| 四轴/五轴 | 学术/作品/表达/现金 + 精力（HUD 隐藏，属性面板可见） |
| 档案 | 玩家产出物的记录（文件夹），驱动简历/结局的个性化 |
| 记录 | 全程行动日志（PlayerState.log），Home 的「记录」页展示 |
| 借来的 | 玩家点[问学长]跳过后，档案条目的标记 borrowed:true |
| 速结行动 | 10 秒内完成的纯数值选修行动，无深度玩法 |
| 本地试玩模式 | 未配置 Supabase 时的降级模式：无登录，存档在 localStorage |
